# Core dependencies
torch>=2.0.0
transformers>=4.40.0
accelerate>=0.28.0
pillow>=10.0.0
flask>=3.0.0
requests>=2.31.0
websockets>=12.0
numpy>=1.24.0
pytest>=7.0.0
python-dotenv>=1.0.0
pyyaml>=6.0.0
tqdm>=4.66.0

# Windows-specific optimizations (experimental on other platforms)
flash-attn>=2.5.0; sys_platform == "win32"
triton>=2.0.0; sys_platform == "win32"

# Unsloth for quantized models
unsloth>=2024.10.0; sys_platform == "win32"

# Optional: chromadb for vector storage (may need separate install on Windows)
# chromadb>=0.4.0

# Optional: qwen-vl-utils (may be included with unsloth)
# qwen-vl-utils>=0.0.3
